apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: mynapp-ing
spec:
  rules:
  - host: cccr.example.com
    http:
      paths:
      - path: /
        backend:
          serviceName: mynapp-svc-ext-np
          servicePort: 80
