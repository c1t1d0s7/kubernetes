## 2. emptyDir 볼륨

### 1) emptyDir 볼륨 소개
EmptyDir 볼륨은 아무 데이터도 없는 빈 디렉토리를 제공해 주는 볼륨이다. 파드가 생성하는 데이터를 저장할 수 있으며, 동일한 파드 내의 컨테이너 간에 데이터를 공유할 때 유용하게 사용할 수 있다. 또한, 빅 데이터와 같은 큰 데이터 셋트를 처리하기 위한 임시 저장소로도 사용 가능하다.

### 2) emptyDir 볼륨 생성
다음은 파드나 컨트롤러에 볼륨을 정의하는 포맷이다.
```yaml
spec:
  containers:
  - volumeMounts:
    - name: <VOL-NAME>
        mountPath: <PATH>
  volumes:
  - name: <VOL-NAME>
    emptyDir: {}
```

- .spec.volumes: 
- .spec.volumes.emptyDir: emptyDir 타입의 볼륨 정의
  * {}: 값을 비워두면 호스트의 로컬 디스크를 사용
  * { Memory }: tmpfs(RAM 기반 파일시스템)을 사용해 고성능의 볼륨을 제공
- .spec.volumes.name: 볼륨의 이름

- .spec.containers.volumeMounts: 컨테이너 마운트 할 볼륨을 선언
- .spec.containers.volumeMounts.name: 사용할 볼륨 이름().spec.volumes 필드와 이름 참조)
- .spec.containers.volumeMounts.mountPath: 컨테이너 내의 마운트 포인트

다음과 같이 EmptyDir 볼륨을 사용하는 레플리카셋을 정의한다.
> myapp-rs-emptydir.yaml
```yaml
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: myapp-rs-fortune
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp-rs-fortune
  template:
    metadata:
      labels:
        app: myapp-rs-fortune
    spec:
      containers:
      - name: web-server
        image: nginx:alpine
        volumeMounts:
        - name: web-fortune
          mountPath: /usr/share/nginx/html
          readOnly: true
        ports:
        - containerPort: 80
      - name: html-generator
        image: ghcr.io/c1t1d0s7/fortune
        volumeMounts:
        - name: web-fortune
          mountPath: /var/htdocs
      volumes:
      - name: web-fortune
        emptyDir: {}
```
web-fortune 이란 이름의 EmptyDir 볼륨을 정의했으며, web-server 컨테이너는 nginx 웹 서버로 이미지로 /usr/share/nginx/html 디렉토리에 볼륨을 마운트 하며, html-generator 컨테이너는 ghcr.io/c1t1d0s7/fortune 이미지를 사용하여 같은 볼륨을 /var/htdocs 디렉토리에 마운트 한다.

> 참고
> ghcr.io/c1t1d0s7/fortune 이미지
> ubuntu 이미지에 fortune 패키지를 설치해 놓은 이미지
> fortune은 명령을 실행할 때마다 랜덤 메시지를 출력
> 3초마다 fortune 명령을 실행하도록 구성됨

함께 사용할 서비스도 생성한다.
> myapp-svc-emptydir.yaml
```yaml
apiVersion: v1
kind: Service
metadata:
  name: myapp-svc-fortune
spec:
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: myapp-rs-fortune
```
포트는 80번 포트이다. nginx 이미지는 기본적으로 80번 포트로 서비스한다.

### 3) emptyDir 볼륨 생성 및 확인
레플리카셋과 서비스를 생성한다.
```
$ kubectl create -f myapp-rs-emptydir.yaml -f myapp-svc-emptydir.yaml

replicaset.apps/myapp-rs-fortune created
service/myapp-svc-fortune created
```

컨트롤러의 상태를 확인한다.
```
$ kubectl get replicasets.apps

NAME               DESIRED   CURRENT   READY   AGE
myapp-rs-fortune   1         1         1       17s
```

파드의 상태를 확인한다. 두 개의 파드가 준비된 것을 확인할 수 있다.
```
$ kubectl get pods

NAME                     READY   STATUS    RESTARTS   AGE
myapp-rs-fortune-fpnqv   2/2     Running   0          18s
```

서비스도 확인한다.
```
$ kubectl get services myapp-svc-fortune

NAME                TYPE           CLUSTER-IP    EXTERNAL-IP      PORT(S)        AGE
myapp-svc-fortune   LoadBalancer   10.233.49.0   192.168.56.200   80:32635/TCP   35s
```

web-server 컨테이너의 /usr/share/nginx/html/index.html 파일을 확인해보자.
```
$ kubectl exec myapp-rs-fortune-fpnqv -c web-server -- cat /usr/share/nginx/html/index.html

Your true value depends entirely on what you are compared with.
```
/usr/share/nginx/html/index.html 파일의 내용은 fortune에 의해서 생성된 것이며, 위와 같이 않을 수 있다. (3초마다 랜덤 한 문장이 생성된다.)

html-generator 컨테이너의 /var/htdocs/index.html 파일을 확인해보자.
```
$ kubectl exec myapp-rs-fortune-fpnqv -c html-generator -- cat /var/htdocs/index.html

Many pages make a thick book, except for pocket Bibles which are on very
very thin paper.
```
아주 빠르게(3초 내) 확인하면 같은 내용이 있는 것을 확인할 수 있다. 이전 컨테이너 내용을 확인한지 3초가 지났다면 다른 내용이 있는 것이 당연하다.

mynapp-svc-fortune 서비스에 접근해 내용을 확인해보자.
```
$ curl http://192.168.56.200

You can rent this space for only $5 a week.
```
```
$ curl http://192.168.56.200

Q:	Why did the programmer call his mother long distance?
A:	Because that was her name.
```

web-server 컨테이너와 html-generator 컨테이너는 같은 emptyDir 볼륨을 공유하고 있으며, html-generator 컨테이너는 컨텐츠를 생성하고, web-server 컨테이너는 컨텐츠를 읽어 외부에 제공해 준다.

### 4) gitRepo 볼륨
> DEPRECATED  
gitRepo 볼륨은 EmptyDir 볼륨에 지정한 Git 리포지토리의 파일을 채워서 제공해 주는 볼륨이다.

다음은 gitRepo 볼륨을 정의하는 예제이다.
```yaml
volumes:
- name: <VOL-NAME>
  gitRepo:
    repository: <GIT-REPO-URL>
    revision: <BRANCH>
    directory: <PATH>
```
- .volumes.gitRepo: gitRepo 볼륨 정의
- .volumes.gitRepo.repository: Git 저장소의 주소
- .volumes.gitRepo.revision: 저장소의 브랜치
- .volumes.gitRepo.directory: 저장소의 특정 경로 지정

### 5) 초기화 컨테이너
gitRepo 볼륨 유형은 더 이상 사용되지 않기 때문에 대신, Git 저장소가 있는 컨테이너를 프로비전 하려면 초기화 컨테이너(InitContainer)에 EmptyDir 볼륨을 마운트하고, 여기에 git 명령을 사용해서 Git 저장소를 복제하고, 파드 컨테이너에서 emptyDir 볼륨을 마운트 한다.

"초기화 컨테이너"는 파드의 "애플리케이션 컨테이너"가 실행되기 전에, 여러 가지 작업을 수행하도록 설정할 수 있는 컨테이너다.

초기화 컨테이너는 애플리케이션 컨테이너가 실행되기 전 딱 한 번만 실행되며, 초기화 컨테이너가 실패 시 성공할 때까지 재시도를 하게 되며, 초기화 컨테이너가 성공하지 전까지는 애플리케이션 컨테이너는 실행되지 않는다.

> 참고  
> 초기화 컨테이너는 레디니스 프로브를 지원하지 않는다.

다음은 초기화 컨테이너를 사용하여 Git 저장소를 제공하는 예제이다.
> myapp-pod-git.yaml
```
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-git
spec:
  initContainers:
    - name: git-clone
      image: alpine/git
      args:
        - clone
        - --single-branch
        - --
        - https://github.com/kubernetes/kubernetes
        - /repo
      volumeMounts:
        - name: git-repository
          mountPath: /repo
  containers:
    - name: git-container
      image: busybox
      args: ['tail', '-f', '/dev/null']
      volumeMounts:
        - name: git-repository
          mountPath: /repo
  volumes:
    - name: git-repository
      emptyDir: {}
```
초기화 컨테이너(.spec.initContainers)는 쿠버네티스 저장소를 Git으로 클론 하며 이를 /repo 디렉터리에 저장하게 된다. /repo 디렉토리는 emptyDir 타입의 볼륨인 git-repository로 제공된다. 초기화 컨테이너가 성공하면, git-container 컨테이너에서 똑같은 git-repository 볼륨을 마운트 하게 된다.

초기화 컨테이너를 가지고 있는 파드를 생성해보자.
```
$ kubectl create -f myapp-pod-git.yaml

pod/myapp-pod-git created
```

곧바로 파드의 상태를 추적 해보자.
```
$ kubectl get pods --watch

NAME            READY   STATUS            RESTARTS   AGE
myapp-pod-git   0/1     Init:0/1          0          11s
myapp-pod-git   0/1     PodInitializing   0          98s
myapp-pod-git   1/1     Running           0          102s
```
파드의 상태가 초기화(Init:0/1)에서 파드 초기화 중(PodInitializing)으로 전환되고, 마지막으로 초기화가 성공하면 애플리케이션 컨테이너가 실행(Running) 상태가 된다.

실제 Git 저장소가 클론 됐는지 확인해보자.
```
$ kubectl exec myapp-pod-git -- ls -l /repo

total 208
drwxr-xr-x    2 root     root          4096 Mar  3 03:53 CHANGELOG
lrwxrwxrwx    1 root     root            19 Mar  3 03:53 CHANGELOG.md -> CHANGELOG/README.md
-rw-r--r--    1 root     root           525 Mar  3 03:53 CONTRIBUTING.md
-rw-r--r--    1 root     root         11358 Mar  3 03:53 LICENSE
drwxr-xr-x    3 root     root          4096 Mar  3 03:53 LICENSES
...
```
/repo 디렉토리에 정상적으로 쿠버네티스 저장소가 클론 되었다.

### 7) 컨트롤러, 파드 및 서비스 삭제
```
$ kubectl delete -f myapp-rs-emptydir.yaml -f myapp-svc-emptydir.yaml -f myapp-pod-git.yaml

pod "myapp-pod-git" deleted
replicaset.apps "myapp-rs-fortune" deleted
service "myapp-svc-fortune" deleted
```
