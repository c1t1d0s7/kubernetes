## 2 서비스 탐색

### 1) 환경변수를 이용한 서비스 탐색
```
kubectl exec mynapp-rs-XXXXX env
```

### 2) DNS를 이용한 서비스 탐색
```
kubectl get all -n kube-system -l k8s-app=kube-dns
```

```
kubectl exec mynapp-rs-XXXXX cat /etc/resolv.conf
```

```
kubectl run nettool -it --image=praqma/network-multitool --generator=run-pod/v1 --rm=true bash

curl http://mynapp-svc

curl http://mynapp-svc.default

curl http://mynapp-svc.default.svc.cluster.local
```

```
<서비스 이름>.<네임스페이스>.svc.<클러스터 도메인>
mynapp-svc.default.svc.cluster.local
```
