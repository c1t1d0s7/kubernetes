## 5. 파드의 생명주기와 프로브

파드는 정의된 순서대로 생명주기를 가진다.
`Pending` -> `Running` -> `Succeeded` 또는 `Failed` 

쿠버네티스 노드의 구성 요소인 kubelet은 파드의 컨테이너 상태를 추적하고, 오류가 발생하면 정상 상태로 만들기 위한 조치로 컨테이너를 재시작할 수도 있다.

파드는 파드가 최초 생성될 때 한 번만 스케줄링 되며, 특정 노드에 할당되면, 파드는 중지되거나 종료될 때까지 항상 같은 노드에서 실행된다.

### 1) 파드의 생명주기
파드는 (영구적으로 사용하기 위한 워크로드 리소스가 아닌) 임시로 사용하기 위한 워크로드 리소스다. 파드가 생성되면 파드를 식별하기 위한 고유의 UID가 할당되고, 종료될 때까지 특정 노드에서만 실행된다.

쿠버네티스는 파드를 일회용으로 간주하며, 상위 레벨의 추상화 리소스인 컨트롤러 리소스를 이용하여 파드를 관리한다.

#### (1) 파드의 단계 확인
파드의 단계는 `kubectl get pods` 명령의 결과 중 STATUS 필드에서 확인하거나, 파드 오브젝트의 `.status.phase` 필드에서 확인할 수 있다.

```
kubectl get pod myapp-pod -o jsonpath='{.status.phase}'

Running
```

> 참고
> JSONPath
> https://kubernetes.io/docs/reference/kubectl/jsonpath/

#### (2) 파드의 단계
- Pending
  * 파드가 클러스터에서 승인되었지만, 실행되고 있지 않음
  * 파드가 스케줄링되기 전
  * 이미지 풀링
- Running
  * 파드가 노드에 할당됨
  * 하나 이상의 컨테이너가 실행 중
  * 시작 또는 재시작 중
- Succeeded
  * 모든 컨테이너가 정상적(zero)으로 종료
  * 재시작되지 않음
- Failed
  * 모든 컨테이너가 종료
  * 하나 이상의 컨터이너가 실패(non-zero)로 종료
- Unknown
  * 알 수 없음
  * 일반적으로 노드와의 통신 오류

> 참고
> 일반적으로 파드 삭제 시 Terminating 상태로 표시되나, 이는 파드의 단계에 해당하지 않음

> 참고
> 파드의 삭제는 유예기간(gracefully)을 두고 종료됨
> 유예기간의 기본값은 30초
> 강제로 즉시 삭제하려면 --force 옵션 사용

### 2) 컨테이너 상태
스케줄러가 파드를 특정 노드에 할당하면, kubelet은 컨테이너 런타임을 이용하여 컨테이너를 생성하며, kubelet은 컨테이너의 상태를 주기적으로 모니터링한다.

#### (1) 컨테이너 상태 확인
kubelet은 파드의 단계뿐만 아니라 각 컨테이너의 상태도 모니터링한다. 

컨테이너의 상태는 `kubectl describe pod ` 명령의 결과 중 Containers 항목이나, 파드 오브젝트의 `.status.containerStatuses[*].state` 필드에서 확인할 수 있다.

```
kubectl get pod myapp-pod -o jsonpath='{.status.containerStatuses[*].state}'

map[running:map[startedAt:2021-03-02T04:09:09Z]]
```

#### (2) 컨테이너 상태
- Waiting
  * 컨테이너가 Running 또는 Terminated 상태가 아닌 상태
  * 컨테이너 시작 전 필요한 작업 중(이미지 풀링, 스토리지 연결 등)
  * Reason 필드에 이유가 표시됨
- Running
  * 실행 중
  * 해당 상태가 된 시각 표시
- Terminated
  * 컨테이너 실행이 완료됨
  * 실패
  * Reason 필드에 이유가 표시됨
  * 해당 상태가 된 시각 표시

### 3) 컨테이너의 재시작 정책
모든 컨테이너는 컨테이너의 오류 발생 시 재시작을 하기 위한 재시작 정책을 가지고 있다.

.spec.restartPolicy 재시작 정책:
- Always: (기본값) 종료/실패 시 항상 재시작
- Onfailure: 실패 시 재시작 (정상 종료 시 재시작하지 않음)
- Never: 재시작 하지 않음

```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
spec:
  restartPolicy: Always
  containers:
  - image: ghcr.io/c1t1d0s7/go-myweb
    name: myapp
    ports:
    - containerPort: 8080
      protocol: TCP
```

#### 4) 지수 백오프 지연 시간
파드의 컨테이너 재시작은 최대 300초로 제한된 지수 백오프(exponential back-off) 지연(10초, 20초, 40초, 80초 ...) 시간을 순차적으로 가지며 재시작한다. 10분 동안 이상 없이 동작하면 지수 백오프 지연 시간을 초기화한다.

### 2) 컨테이너 프로브
컨테이너 프로브는 kubelet이 컨테이너를 주기적으로 진단하는 프로브 핸들러를 호출한다. 

프로브의 핸들러는 세 가지 메커니즘을 가지고 컨테이너의 상태를 진단한다.
- HTTPGetAction
  * 특정 경로에 HTTP GET 요청
  * HTTP 응답 코드가 2XX 또는 3XX 인지 확인함
- TCPSocketAction
  * 특정 TCP 포트 연결
  * 포트가 활성화되어 있는지 확인
- ExecAction
  * 컨테이너 내의 지정된 바이너리를 실행
  * 명령의 종료 코드가 0인지 확인

프로브의 핸들러는 세 가지 상태 중 하나는 가진다.
- Success: 진단 통과
- Failure: 진단에 실패
- Unknown: 진단 자체가 실패

프로브는 세 가지 종류를 가진다.
- livenessProbe
  * 컨테이너가 동작 중인지 확인
  * 진단에 실패하면 재시작 정책을 적용
  * livenessProbe를 선언하지 않으면, 기본 상태는 Success
- readinessProbe
  * 컨테이너가 요청을 처리할 준비가 되었는지 확인
  * 진단에 실패하면 엔드포인트(Endpoint) 컨트롤러는 파드의 IP 주소를 엔드포인트에서 제거
  * readinessProbe를 선언하지 않으면, 기본 상태는 Success
- startupProbe
  * 컨테이너 내의 애플리케이션이 시작되었는지 확인
  * startupProbe가 선언되었을 경우, 진단을 통과하기 전까지 다른 프로브를 활성화하지 않음 

#### (1) 프로브 정의
```yaml
...
spec:
  containers:
  - [ livenssProbe | readinessProbe | startupProbe ]:
      httpGet:
        path: /health
        port: 8080
        ...
      tcpSocket:
        port: 53
        ...
      exec:
        command: ['mysqladmin', 'ping']
        ...
...
```

- .spec.containers.*Probe: 라이브니스 프로브 정의
- .spec.containers.*Probe.httpGet: HTTP GET 프로브 정의
- .spec.containers.*Probe.tcpSocket: TCP 소켓 프로브 정의
- .spec.containers.*Probe.exec: Exec 프로브 정의


#### (2) 라이브니스 프로브 - 정상
다음은 라이브니스 프로브를 정의한 파드 오브젝트다.

> myapp-pod-liveness.yaml
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-liveness
spec:
  containers:
  - image: ghcr.io/c1t1d0s7/go-myweb
    name: myapp
    ports:
    - containerPort: 8080
      protocol: TCP
    livenessProbe:
      httpGet:
        path: /health
        port: 8080
```
HTTP GET 프로브를 사용하였으며, 경로는 / 포트는 8080이다. 즉, 결론적으로 200 코드 응답이 돌아올 것으로 예상할 수 있다.

파드 오브젝트를 생성한다.
```
$ kubectl create -f myapp-pod-liveness.yaml

pod/myapp-pod-liveness created
```

파드 상태를 주기적으로 확인한다.
```
kubectl get pods --watch

NAME                 READY   STATUS    RESTARTS   AGE
myapp-pod-liveness   1/1     Running   0          39s
```
계속 파드의 상태를 모니터링을 할 것이므로 다음 작업부터 새로운 터미널에서 실행한다.

> 참고
> 유닉스 계열은 watch 명령을 이용하여 모니터링할 수 있다.
> $ watch -n1 -d <COMMAND>



#### (3) 라이브니스 프로브 - 비정상
다음은 의도적으로 프로브의 상태를 실패로 만들기 위한 파드 오브젝트다.

> myapp-pod-liveness-404.yaml
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-liveness-404
spec:
  containers:
  - image: ghcr.io/c1t1d0s7/go-myweb
    name: myapp
    ports:
    - containerPort: 8080
      protocol: TCP
    livenessProbe:
      httpGet:
        path: /health?code=404
        port: 8080
```

파드 오브젝트를 생성한다.
```
$ kubectl create -f myapp-pod-liveness-404.yaml

pod/myapp-pod-liveness-404 created
```

모니터링을 하고 있는 터미널에 표시된 파드의 상태를 확인한다.
```
NAME                     READY   STATUS              RESTARTS   AGE
...
myapp-pod-liveness-404   0/1     Pending             0          0s
myapp-pod-liveness-404   0/1     ContainerCreating   0          0s
myapp-pod-liveness-404   1/1     Running             0          3s
myapp-pod-liveness-404   1/1     Running             1          29s
myapp-pod-liveness-404   1/1     Running             2          59s
myapp-pod-liveness-404   1/1     Running             3          89s
...
```
시간이 조금 흘러 확인해보면 RESTARTS(재시작) 필드가 0에서 양수로 변경된 것을 확인할 수 있다. 이는 라이브니스 프로브에 의해 컨테이너가 건강한 상태가 아님을 감지하고 재시작을 시도했다는 의미이다.

```
myapp-pod-liveness-404   1/1     Running             4          119s
myapp-pod-liveness-404   0/1     CrashLoopBackOff    4          2m27s
myapp-pod-liveness-404   1/1     Running             5          3m24s
```
시간이 조금 더 흘러 확인해 보면 상태가 CrashLoopBackOff 상태인 것을 확인할 수 있으며, 이는 지수 백오프 지연이다.

kubectl describe 명령으로 파드의 상세정보를 확인한다.
```
$ kubectl describe pod myapp-pod-liveness-404

Name:         myapp-pod-liveness-404
...
Containers:
  myapp:
...
    State:          Waiting
      Reason:       CrashLoopBackOff
    Last State:     Terminated
      Reason:       Error
      Exit Code:    2
      Started:      Tue, 02 Mar 2021 05:19:05 +0000
      Finished:     Tue, 02 Mar 2021 05:19:33 +0000
    Ready:          False
    Restart Count:  6
    Liveness:       http-get http://:8080/health%3Fcode=404 delay=0s timeout=1s period=10s #success=1 #failure=3
...
Events:
  Type     Reason     Age                    From               Message
  ----     ------     ----                   ----               -------
  Normal   Scheduled  6m27s                  default-scheduler  Successfully assigned default/myapp-pod-liveness-404 to kube-node2
  Normal   Pulled     5m28s (x3 over 6m25s)  kubelet            Successfully pulled image "ghcr.io/c1t1d0s7/go-myweb"
  Normal   Created    5m28s (x3 over 6m25s)  kubelet            Created container myapp
  Normal   Started    5m28s (x3 over 6m25s)  kubelet            Started container myapp
  Normal   Pulling    5m (x4 over 6m26s)     kubelet            Pulling image "ghcr.io/c1t1d0s7/go-myweb"
  Warning  Unhealthy  5m (x9 over 6m20s)     kubelet            Liveness probe failed: HTTP probe failed with statuscode: 404
  Normal   Killing    5m (x3 over 6m)        kubelet            Container myapp failed liveness probe, will be restarted
  Warning  BackOff    79s (x11 over 4m)      kubelet            Back-off restarting failed container
```

Containers의 Liveness 필드의 의미:
- delay
  - 프로브를 초기화하기 전 지연
  - 기본값: 0초
  - .spec.containers.*Probe.initialDelaySeconds
- timeout
  - 프로브 타임아웃
  - 기본값 1초
  - .spec.containers.*Probe.timeoutSeconds
- period
  - 프로브 주기
  - 기본값 10초
  - .spec.containers.*Probe.periodSeconds
- success
  - 진단 성공 임계값
  - 기본값 1
  - .spec.containers.*Probe.successThreshold
- failure
  - 진단 실패 임계값
  - 기본값 3 (3번 연속 진단 실패 시 최종 실패로 진단)
  - .spec.containers.*Probe.failureThreshold

라이브니스 프로브의 경우 초기 지연 시간(initialDelaySeconds)을 설정하는 것이 중요한데, 파드(컨테이너)가 실행되고 난 이후 애플리케이션에 따라 애플리케이션이 정상 동작하는 데 시간이 걸릴 수 있기 때문이다. 간혹 초기 지연 시간을 설정하지 않아 계속적인 재시작을 초래할 수 있다.

####(4) 스타트업 프로브 - 정상
다음은 스타트업 프로브를 정의한 파드 오브젝트다.
> myapp-pod-startup.yaml
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-startup
spec:
  containers:
  - image: ghcr.io/c1t1d0s7/go-myweb
    name: myapp
    ports:
    - containerPort: 8080
      protocol: TCP
    startupProbe:
      httpGet:
        path: /health
        port: 8080
    livenessProbe:
      httpGet:
        path: /health
        port: 8080
```

파드를 생성한다.
```
$ kubectl create -f myapp-pod-startup.yaml

pod/myapp-pod-startup created
```

파드의 상세정보를 확인한다.
```
$ kubectl describe pod myapp-pod-startup

Name:         myapp-pod-startup
...
Containers:
  myapp:
...
    State:          Running
      Started:      Tue, 02 Mar 2021 05:45:32 +0000
    Ready:          True
    Restart Count:  0
    Liveness:       http-get http://:8080/health delay=0s timeout=1s period=10s #success=1 #failure=3
    Startup:        http-get http://:8080/health delay=0s timeout=1s period=10s #success=1 #failure=3
...
```

####(5) 스타트업 프로브 - 비정상
다음은 의도적으로 프로브의 상태를 실패로 만들기 위한 파드 오브젝트다.

> myapp-pod-startup-404.yaml
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod-startup-404
spec:
  containers:
  - image: ghcr.io/c1t1d0s7/go-myweb
    name: myapp
    ports:
    - containerPort: 8080
      protocol: TCP
    startupProbe:
      httpGet:
        path: /health?code=404
        port: 8080
    livenessProbe:
      httpGet:
        path: /health
        port: 8080
```

파드를 생성한다.
```
$ kubectl create -f myapp-pod-startup-404.yaml

pod/myapp-pod-startup-404 created
```

파드의 상태를 확인한다.
```
NAME                    READY   STATUS              RESTARTS   AGE
...
myapp-pod-startup-404   0/1     Pending             0          0s
myapp-pod-startup-404   0/1     ContainerCreating   0          0s
myapp-pod-startup-404   0/1     Running             0          3s
myapp-pod-startup-404   0/1     Running             1          92s
...
```
파드의 상태가 Running 상태이지만 준비(READY 0/1)가 되지 않고 있다.

파드의 상세 정보를 확인한다.
```
$ kubectl describe pod myapp-pod-startup-404

Name:         myapp-pod-startup-404
...
Containers:
  myapp:
...
    State:          Running
      Started:      Tue, 02 Mar 2021 06:02:15 +0000
    Last State:     Terminated
      Reason:       Error
      Exit Code:    2
      Started:      Tue, 02 Mar 2021 06:00:45 +0000
      Finished:     Tue, 02 Mar 2021 06:02:13 +0000
    Ready:          False
    Restart Count:  1
    Liveness:       http-get http://:8080/health delay=0s timeout=1s period=10s #success=1 #failure=3
    Startup:        http-get http://:8080/health%3Fcode=404 delay=0s timeout=1s period=10s #success=1 #failure=3
...
Conditions:
  Type              Status
  Initialized       True
  Ready             False
  ContainersReady   False
  PodScheduled      True
...
Events:
  Type     Reason     Age                  From               Message
  ----     ------     ----                 ----               -------
...
  Warning  Unhealthy  18s (x6 over 2m8s)   kubelet            Startup probe failed: HTTP probe failed with statuscode: 404
```
스타트업 프로브에 실패한 것으로 확인된다. 스타트업 프로브에 실패하면 라이브니스 프로브는 활성화되지 않는다.

####(6) 리소스 삭제
불필요한 파드를 삭제한다.
```
$ kubectl delete pod --all

pod "myapp-pod-liveness" deleted
pod "myapp-pod-liveness-404" deleted
pod "myapp-pod-startup" deleted
pod "myapp-pod-startup-404" deleted
```
--all 옵션은 모든 리소스를 대상으로 한다. 주의해서 사용해야 하지만 간혹 모든 리소스를 대상으로 일괄적으로 적용해야 하는 경우 유용하다.
