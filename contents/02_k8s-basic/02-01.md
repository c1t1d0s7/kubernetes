## 1. 컨테이너 이미지 만들기

### 1) Node.js 애플리케이션
> index.js
```javascript
const http = require('http');
const os = require('os');
const ip = require('ip');
const dns = require("dns");

console.log(Date());
console.log("...Start My Node.js Application...");

var handler = function(request, response) {
    console.log(Date());
    console.log("Received Request From " + request.connection.remoteAddress);
    response.writeHead(200);
    response.write("Message: " + process.argv[2] + "\n");
    response.write("Hostname: " + os.hostname() + "\n");
    response.write("Platform: " + os.platform() + "\n");
    response.write("Uptime: " + os.uptime() + "\n");
    response.write("IP: " + ip.address() + "\n");
    response.write("DNS: " + dns.getServers() + "\n");
    response.end();
};

var www = http.createServer(handler);
www.listen(8080);
```
### 2) 사용자 지정 이미지 생성용 Dockerfile
> Dockerfile
```dockerfile
FROM node:slim
WORKDIR /usr/src/app
COPY index.js . 
RUN npm install ip
ENTRYPOINT ["node", "index.js"]
CMD ["Hello World!"]
EXPOSE 8080/tcp
```

### 3) 사용자 지정 이미지 빌드
```
docker build -t myweb .
```

```
docker image ls
```

### 4) 사용자 지정 이미지 계층 확인
```
docker image inspect node:slim
```

```
docker image inspect myweb:latest
```

### 5) 사용자 지정 이미지 실행
```
docker run -d -p 8081:8080 myweb:latest
```

```
curl http://localhost:8081
```

### 6) 사용자 지정 이미지 업로드
```
docker login
```

```
docker image tag myweb:latest <ACCOUNT>/myweb:latest
```

```
docker push <ACCOUNT>/myweb:latest
```